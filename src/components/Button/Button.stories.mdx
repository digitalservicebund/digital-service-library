import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import Button from './Button';
import CircleIcon from '../Icons/Circle';

<Meta title="Atoms/Button" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button

[Source code](https://github.com/digitalservice4germany/digital-service-library/tree/main/src/components/Button) | [Figma](https://www.figma.com/file/49Gxm2IajuwbGhj37zu9x0/Angie?node-id=0%3A1)

`<Button>` is a drop-in replacement for `<button>`. You can use all the properties that you are used to. Only exception is `className`: when you set `className`, your value will be appended to the existing `className` of `<Button>`. This is handy to add margins, for example. In addition you can use the properties from the table below to configure the visual appearance. Use either `secondary` or `tertiary`, not both. Use either `small` or `tiny`, not both.

<ArgsTable of={Button} />

<Canvas>
  <Story
    name="Example"
    args={{
      small: true,
      type: "submit",
      className: "mb-2",
      onClick: () => alert('clicked'),
      children: "Example"
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Links which look like buttons

The component is overloaded. If you pass a `href` property and optionally other properties from `<a>`, the `<Button>` component will render an `<a>` link and not a `<button>`. The important piece is, that you pass a `href` property. Obviously you can then only use properties from `<a>`.

<Canvas>
  <Story
    name="Link"
    args={{
      href: "https://digitalservice.bund.de",
      target: "_blank",
      rel: "noopener",
      children: "This is a link"
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Visual overview

### Primary button (`<Button>`)

<Canvas>
  <Story
    name="Primary"
    args={{
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Primary Disabled"
    args={{
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Primary with Icon"
    args={{
      children: "Registrieren",
      icon: <CircleIcon />
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Secondary button (`<Button secondary>`)

<Canvas>
  <Story
    name="Secondary"
    args={{
      secondary: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary Disabled"
    args={{
      secondary: true,
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary with Icon"
    args={{
      secondary: true,
      children: "Registrieren",
      icon: <CircleIcon />
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Tertiary button (`<Button tertiary>`)

<Canvas>
  <Story
    name="Tertiary"
    args={{
      tertiary: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Tertiary Disabled"
    args={{
      tertiary: true,
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Tertitary with Icon"
    args={{
      tertiary: true,
      children: "Registrieren",
      icon: <CircleIcon />
    }}>
    {Template.bind({})}
  </Story>
</Canvas>


### Primary Small button (`<Button small>`)

<Canvas>
  <Story
    name="Primary Small"
    args={{
      small: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Primary Small Disabled"
    args={{
      small: true,
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Primary Small with icon"
    args={{
      small: true,
      children: "Registrieren",
      icon: <CircleIcon />
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Secondary Small button (`<Button secondary small>`)

<Canvas>
  <Story
    name="Secondary Small"
    args={{
      small: true,
      secondary: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary Small Disabled"
    args={{
      small: true,
      secondary: true,
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Tertiary Small button (`<Button tertiary small>`)

<Canvas>
  <Story
    name="Tertiary Small"
    args={{
      small: true,
      tertiary: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Tertiary Small Disabled"
    args={{
      small: true,
      tertiary: true,
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
</Canvas>


### Primary Tiny button (`<Button tiny>`)

<Canvas>
  <Story
    name="Primary Tiny"
    args={{
      tiny: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Primary Tiny Disabled"
    args={{
      tiny: true,
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Primary Tiny with icon"
    args={{
      tiny: true,
      children: "Registrieren",
      icon: <CircleIcon />
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Secondary Tiny button (`<Button secondary tiny>`)

<Canvas>
  <Story
    name="Secondary Tiny"
    args={{
      tiny: true,
      secondary: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary Tiny Disabled"
    args={{
      tiny: true,
      secondary: true,
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Tertiary Tiny button (`<Button tertiary tiny>`)

<Canvas>
  <Story
    name="Tertiary Tiny"
    args={{
      tiny: true,
      tertiary: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Tertiary Tiny Disabled"
    args={{
      tiny: true,
      tertiary: true,
      disabled: true,
      children: "Registrieren"
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Icon only

Do we need this?

<Canvas>
  <Story
    name="Primary icon only"
    args={{
      icon: <CircleIcon />,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Primary small icon only"
    args={{
      small: true,
      icon: <CircleIcon />,
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Primary tiny icon only"
    args={{
      tiny: true,
      icon: <CircleIcon />,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Edge cases

### Long text

Given a small device and a long button text it might happen that the text does not fit into one line.

<Canvas>
  <Story
    name="Long text"
    args={{
      children: "Der DigitalService4Germany entwickelt digitale Lösungen für und mit der Bundesverwaltung. Im Zentrum stehen die Bedürfnisse der Nutzerinnen und Nutzer. Unser Ziel: Dienstleistungen, die besser für alle funktionieren."
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Long word

Some German words are really long and might not fit in one line on a small device. The word should be broken in several lines even if the breaking point is not optimal.

<Canvas>
  <Story
    name="Long word"
    args={{
      children: "DerDigitalService4GermanyentwickeltdigitaleLösungenfürundmitderBundesverwaltung.ImZentrumstehendieBedürfnissederNutzerinnenundNutzer.UnserZiel:Dienstleistungen,diebesserfürallefunktionieren."
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Long word and icon"
    args={{
      icon: <CircleIcon />,
      children: "DerDigitalService4GermanyentwickeltdigitaleLösungenfürundmitderBundesverwaltung.ImZentrumstehendieBedürfnissederNutzerinnenundNutzer.UnserZiel:Dienstleistungen,diebesserfürallefunktionieren."
    }}>
    {Template.bind({})}
  </Story>
</Canvas>


## A11y

TODO


## Resources

* [GOV.UK Design System: Button component (GitHub issue)](https://github.com/alphagov/govuk-design-system-backlog/issues/34)
